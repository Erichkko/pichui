<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pichui.news">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:name="com.pichui.news.app.MyApp"
        android:theme="@style/AppTheme.Slide">
        <activity android:name=".ui.activity.SplashActivity"
            android:theme="@style/AppTheme"
           >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.Test1Activity" />
        <activity android:name=".ui.activity.TestGridActivity" />
        <activity android:name=".ui.activity.TestMainActivity" />
        <activity android:name=".ui.activity.MainActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".ui.activity.WebViewActivity" />
        <activity android:name=".ui.base.NewsDetailBaseActivity" />
        <activity android:name=".ui.activity.NewsDetailActivity" />
        <activity android:name=".ui.activity.ImageViewPagerActivity" />

        <service android:name=".ui.service.DownloadIntentService" />
    </application>

</manifest>